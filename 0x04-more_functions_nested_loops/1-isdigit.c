#include "holberton.h"

/**
 * _isupper - Uppercase checker.
 * @c: variable to be checked.
 * Return: to 1 if uppercase.
 */

int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
