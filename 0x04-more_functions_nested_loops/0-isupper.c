#include "holberton.h"

/**
 * isuper - Uppercase checker.
 * @c: variable to be checked.
 * Return: to 1 if uppercase.
 */

int _isupper(int c)
{
	if (c >= 65 &&c <= 90)
	{
		return (1);
		return (0);
	}
}
