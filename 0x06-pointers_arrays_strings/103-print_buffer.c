#include "holberton.h"
/**
 * print_buffer - buffer.
 * @*b: string1.
 * @size: string2
 * Return: String with all letters in ROT13 base.
 */
void print_buffer(char *b, int size);
{
	char *b = b - 1;
	int size = &b;
	if (size == b)
	{
       
	}
}
